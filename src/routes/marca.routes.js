import { Router } from 'express';

const router = Router()
 
router.get('/', async(req,res) =>{
    try {
        //Para esto enecesito primero el modelo marca.model.js
    } catch (error) {
        
    }
})